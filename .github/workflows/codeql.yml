image: maven:3-openjdk-15

stages:
  - build

build:
  stage: build
  script: mvn clean install -Powasp-dependency-check
  artifacts:
    paths:
      - target/dependency-check-report.html
    when:
      always		

